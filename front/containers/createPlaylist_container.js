import {connect} from 'react-redux';

import CreatePlaylist from '../components/createplaylist';


const mapStateToProps = state => (

)

export CreatePlaylistContainer = connect(mapStateToProps)(CreatePlaylist);
